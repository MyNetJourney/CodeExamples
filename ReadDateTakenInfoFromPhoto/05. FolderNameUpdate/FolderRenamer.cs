using System.Collections.Generic;
using System.IO;

namespace ReadDateTakenInfoFromPhoto
{
    internal class FolderRenamer
    {
        public void Rename()
        {
            // rename attempt
            foreach (var sortedFolder in new List<string>()) //TODO:
            {
                string basePath = "";
                string newPath = "";

                Directory.Move(basePath, newPath);
            }
        }

    }
}